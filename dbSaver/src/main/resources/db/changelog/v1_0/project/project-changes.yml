databaseChangeLog:
  - changeSet:
      id: project-1
      author: gromov-nikita
      changes:
        - createTable:
            tableName: project
            columns:
              - column:
                  name: id
                  type: int
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: varchar(20)
                  constraints:
                    nullable: false
              - column:
                  name: description
                  type: varchar(200)
                  constraints:
                    nullable: true
              - column:
                  name: domain
                  type: domain_enum
                  constraints:
                    nullable: false
